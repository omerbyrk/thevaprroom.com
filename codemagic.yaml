# Automatically generated on 2022-06-18 UTC from https://codemagic.io/app/62adf6934f03036a9ab3650f/settings
# Note that this configuration is not an exact match to UI settings. Review and adjust as necessary.

workflows:
  default-workflow:
    name: Default Workflow
    max_build_duration: 30
    environment:
      flutter: default
      xcode: latest
      cocoapods: default
    scripts:
      - sh upload_appetize.sh
    artifacts:
      - build/**/outputs/apk/**/*.apk
      - build/**/outputs/bundle/**/*.aab
      - build/**/outputs/**/mapping.txt
      - build/ios/iphoneos/Runner.app
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log
      - '*.snap'
      - build/windows/**/*.msix
      - flutter_drive.log
    publishing:
      email:
        recipients:
          - omerbyrk8@gmail.com
